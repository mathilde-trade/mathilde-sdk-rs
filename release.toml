allow-branch = ["main", "!HEAD"]

shared-version = true
publish = false
push = true
push-remote = "origin"

tag = true
tag-name = "msdk-v{{version}}"
tag-message = "chore: Release msdk-v{{version}}"

pre-release-commit-message = "chore: Release msdk-v{{version}}"

sign-commit = false
sign-tag = false

allow-branch = ["main", "!HEAD"]

shared-version = true
publish = false
push = true
push-remote = "origin"

tag = true
tag-name = "reg-v{{version}}"
tag-message = "chore: Release reg-v{{version}}"

pre-release-commit-message = "chore: Release reg-v{{version}}"

sign-commit = false
sign-tag = false

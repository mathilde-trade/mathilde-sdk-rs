allow-branch = ["main", "!HEAD"]

shared-version = true
publish = false
push = true
push-remote = "origin"

tag = true
tag-name = "msdk-rs-v{{version}}"
tag-message = "chore: Release msdk-rs-v{{version}}"

pre-release-commit-message = "chore: Release msdk-rs-v{{version}}"

sign-commit = false
sign-tag = false

// Generated by bin/generate_sdk_primitives_bundle.rs.
// Source authority: compute-primitives domain processor contract + feed proto authority.
// Purpose: generated primitives processor family enum.
// Do not edit by hand.

use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum ProcessorFamily {
    BasicStats,
    Channels,
    Correlation,
    Directionality,
    Dispersion,
    Drawdown,
    Extrema,
    Memory,
    MovingAverages,
    Oscillators,
    Range,
    Regression,
    Returns,
    Seasonality,
    Signal,
    Sizing,
    Stops,
    VolumeFlow,
}

impl ProcessorFamily {
    pub const ALL: [Self; 18] = [
        Self::BasicStats,
        Self::Channels,
        Self::Correlation,
        Self::Directionality,
        Self::Dispersion,
        Self::Drawdown,
        Self::Extrema,
        Self::Memory,
        Self::MovingAverages,
        Self::Oscillators,
        Self::Range,
        Self::Regression,
        Self::Returns,
        Self::Seasonality,
        Self::Signal,
        Self::Sizing,
        Self::Stops,
        Self::VolumeFlow,
    ];

    pub const fn canonical_name(self) -> &'static str {
        match self {
            Self::BasicStats => "basic_stats",
            Self::Channels => "channels",
            Self::Correlation => "correlation",
            Self::Directionality => "directionality",
            Self::Dispersion => "dispersion",
            Self::Drawdown => "drawdown",
            Self::Extrema => "extrema",
            Self::Memory => "memory",
            Self::MovingAverages => "moving_averages",
            Self::Oscillators => "oscillators",
            Self::Range => "range",
            Self::Regression => "regression",
            Self::Returns => "returns",
            Self::Seasonality => "seasonality",
            Self::Signal => "signal",
            Self::Sizing => "sizing",
            Self::Stops => "stops",
            Self::VolumeFlow => "volume_flow",
        }
    }

    pub const fn aliases(self) -> &'static [&'static str] {
        match self {
            Self::BasicStats => &["bs", "basic_stats", "basic-stats"],
            Self::Channels => &["ch", "channels"],
            Self::Correlation => &["corr", "correlation"],
            Self::Directionality => &["dir", "directionality"],
            Self::Dispersion => &["disp", "dispersion"],
            Self::Drawdown => &["dd", "drawdown"],
            Self::Extrema => &["ex", "extrema"],
            Self::Memory => &["me", "memory"],
            Self::MovingAverages => &["ma", "moving_averages", "moving-averages"],
            Self::Oscillators => &["osc", "oscillators"],
            Self::Range => &["rng", "range"],
            Self::Regression => &["re", "regression"],
            Self::Returns => &["ret", "returns"],
            Self::Seasonality => &["sea", "seasonality"],
            Self::Signal => &["sig", "signal"],
            Self::Sizing => &["sz", "sizing"],
            Self::Stops => &["st", "stops"],
            Self::VolumeFlow => &["vf", "volume_flow", "volume-flow"],
        }
    }

    pub fn parse(name: &str) -> Option<Self> {
        Self::ALL
            .into_iter()
            .find(|family| family.aliases().contains(&name))
    }
}
